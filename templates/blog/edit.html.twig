{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <h2>Création d'article</h2>
    {{ form_start(form) }}

    {{ form_row(form.title)}}
    {{ form_row(form.content)}}
    {{ form_row(form.image)}}

    <input type="submit" class="btn btn-success" value="Modifier l'article">

    {{ form_end(form) }}
{% endblock %}